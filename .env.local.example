# Database
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# Hub URL (used by MCP server and extension)
HUB_URL="http://localhost:3000"

# GitHub OAuth (optional — required for sign-in, not required for browsing)
# Create an app at https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
AUTH_SECRET= # generate with: openssl rand -base64 32

# MCP Server API key (optional — get from /settings after signing in)
HUB_API_KEY=
